# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
.env.local
.env.development
.env.staging
.env.production

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Sensitive files and credentials
# API keys and secrets
*.key
*.pem
*.p12
*.pfx
*.crt
*.cer
*.jks
secrets.json
config/secrets.js
config/credentials.js

# Database and backend configurations
database.json
db-config.json
backend-config.json
server-config.json
api-endpoints.json
backend-urls.json
server-endpoints.json

# Authentication and OAuth
auth-config.json
oauth-secrets.json
jwt-secrets.json
session-secrets.json

# Third-party service configurations
opensign-config.json
parse-server-config.json
mongodb-config.json
aws-config.json
firebase-config.json

# OpenSign specific files
.env.opensign
opensign-credentials.json
opensign-secrets.json

# Log files with potential sensitive data
*.log
logs/
error.log
access.log
debug.log

# Test files that might contain sensitive data
test-credentials.json
test-config.json
test-endpoints.json
test-secrets.json

# Backup files that might contain sensitive data
*.backup
*.bak
*.tmp
*.swp
*.swo
*~

# IDE and editor files that might contain sensitive paths
.vscode/settings.json
.idea/
*.sublime-workspace
*.sublime-project

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
